<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- CustomizeActivity your theme here. -->
    </style>

    <!-- style for empty list view items -->
    <style name="emptyListViewItem">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@color/background_material_dark</item>
        <item name="android:textColor">@color/primary_text_default_material_dark</item>
    </style>

    <!-- base style for list views -->
    <style name="listViewStyleBase">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:choiceMode">singleChoice</item>
        <item name="android:background">@color/background_material_dark</item>
    </style>

    <!-- style for list views when not on a large or sw600dp device -->
    <style name="listViewStyle" parent="listViewStyleBase"></style>

</resources>
